---
# DEPRECATED — host-side workstation setup (local venv + ansible-playbook).
#
# APME deploy runs in AAP via Product Demos EE. Use:
#   aap-demo enable apme-eap
#
# For local playbook debugging, run deploy_apme_portal.yml inside an EE or with
# collections from execution-environment/requirements.yml.

- name: Deprecated — use AAP-native deploy
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Fail with migration instructions
      ansible.builtin.fail:
        msg: >-
          playbooks/setup.yml is deprecated. Host-side Ansible is not used for apme-eap deploy.
          Run: aap-demo enable apme-eap
          See addons/apme-eap/README.md and docs/adr/023-apme-openshift-template.md
