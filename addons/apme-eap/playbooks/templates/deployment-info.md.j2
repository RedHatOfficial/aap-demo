# Ansible Quality (APME) automation portal deployment

Deployment date: {{ ansible_date_time.iso8601 }}

## Portal access

- **Portal URL:** {{ portal_url }}
- **OpenShift namespace:** {{ openshift_project_name }}
- **Cluster domain:** {{ openshift_cluster_domain }}

## AAP integration

- **AAP host:** {{ aap_host_normalized | default(aap_host) }}
- **Organization:** {{ aap_organization }}
- **OAuth application:** {{ aap_apme_prerequisites_oauth_application_name }}
- **Redirect URI:** {{ portal_url }}/api/auth/rhaap/handler/frame
- **GitHub OAuth callback:** {{ portal_url }}/api/auth/github/handler/frame (optional: confirm on github.com)

## Portal hub

- **Image:** {{ portal_hub_image }}
- **Plugins:** Baked into the container image (no OCI push or install-dynamic-plugins at deploy time)

## Automation Hub (setup-pah)

- **setup-pah credentials:** {{ 'yes' if apme_pah_credentials_available | default(false) else 'no' }}
- **PAH catalog sync:** {{ 'enabled' if apme_pah_collections_sync_enabled | default(false) else 'disabled' }}
- **APME galaxy servers seeded:** {{ 'yes' if apme_pah_seed_apme_galaxy_servers | default(false) else 'no' }}

## Helm releases

| Release | Chart | Version |
| --- | --- | --- |
| {{ portal_helm_release_name }} | {{ portal_helm_chart_name }} | {{ portal_helm_chart_version }} |
| {{ apme_helm_release_name }} | {{ apme_helm_chart_name }} | {{ apme_helm_chart_version }} |

## Verification

```bash
oc get pods -n {{ openshift_project_name }}
oc get route -n {{ openshift_project_name }}
oc get secret secrets-rhaap-portal -n {{ openshift_project_name }}
```

## Next steps

1. Sign in to the automation portal via AAP OAuth.
2. Register a repository (public repos support direct register).
3. Open the **Quality** tab and run a scan on the default branch.
