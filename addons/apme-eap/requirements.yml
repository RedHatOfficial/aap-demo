---
# Ansible Collection Requirements for APME Playbook Addon
# Install with: ansible-galaxy collection install -r requirements.yml

collections:
  - name: kubernetes.core
    version: ">=2.4.0"

  - name: community.okd
    version: ">=2.3.0"

  - name: community.general
    version: ">=6.0.0"
